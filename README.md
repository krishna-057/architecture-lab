# Architecture Lab

Five full-stack architecture projects built over 30 days, focused on interview-grade engineering depth without unnecessary overengineering.

## Projects

1. `FlashReserve`: high-concurrency flash sale reservations.
2. `PocketSentinel`: phone-as-CCTV object detection.
3. `PersonaBridge`: personal AI partner with chat and calls.
4. `CollabFlow`: local-first collaborative workspace.
5. `HookRelay`: webhook delivery and replay platform.

Current workspace:

```text
K:\AutoPilot_Projects\FlashReserve
```

Start with `MASTER_PLAN.md`, then read `WORKFLOW.md`, `SETUP.md`, and `NOTIFICATIONS.md`.
